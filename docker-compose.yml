version: "3.8"

services:
  db:
    image: mariadb:10.11
    container_name: dispensa_db
    restart: always
    env_file:
      - .env
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: